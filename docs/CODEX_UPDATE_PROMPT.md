# 給 Codex 的固定更新指令

將新資料檔放進 `codekok/quanta-dashboard` 專案後，把以下指令貼給 Codex：

```text
請更新 codekok/quanta-dashboard 的「廣達游於智教育影響力地圖」。

先完整讀取根目錄 AGENTS.md、docs/SCHOOL_NAME_RULES.md 與 docs/DATA_UPDATE_SOP.md，再開始處理。

本次以我提供的新資料為新增或修正來源，保留既有歷史紀錄，不可直接覆蓋或刪除舊資料。先比對並回報：
1. 新增紀錄
2. 修正紀錄
3. 同屆同校重複
4. 跨屆疑似同校異名
5. 同名但可能不同校
6. 統計數字可能產生的變化

校名必須依既定規則處理：
- 大學附設實驗學校使用「附設實驗小學」
- 只有「國民小學」縮寫為「國小」
- 原名為「小學」或「小學部」者保持原樣
- 「國中小」統一為「國民中小學」
- 一般校名不顯示縣市，大學附設學校保留完整正式名稱
- 臺南市同名學校保留行政區
- 不確定是否同校時先問我，不可自行合併

確認資料無歧義後再更新。完成後執行資料檢查、Lint，測試桌機與手機版的搜尋、篩選、分頁、地圖及跨屆紀錄。回報更新前後的學校數、校次、地區數及差異原因，先提供可檢查版本，不要直接發布。收到我明確回覆「同意發布」後才能發布。
```
